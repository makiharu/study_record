<!--toaster導入-->
<!--scssを使用している場合は、@import toastも忘れずに-->

<!--toastrで扱うメソッドは :info :success :error :warning　の４つ。
ただし、flashのシンボルは :alertと　:noticeなので、変換して上げる必要がある-->

<div class="container">
  <div class="row">
    <div class="flash-messages">

      <% flash.each do |key, value| %>
        <p><% key = 'success' if key == 'notice' %></p>
        <p><% key = 'error' if key == 'alert' %></p>
        <%= content_tag :div, value, class: "'alert alert-#{key}" %>

      <% end %>
    </div>
  </div>
</div>


